const {
  pipe,
  compose,
  zip,
  intersperse,
  insert,
  flatten,
  flatMap,
  includes,
  compact,
  arrayIntoObject
} = require('./functions');

test('pipe', () => {});
test('compose', () => {});
test('zip', () => {});
test('intersperse', () => {});
test('insert', () => {});
test('flatten', () => {});
test('flatMap', () => {});
test('includes', () => {});
test('compact', () => {});
test('arrayIntoObject', () => {});
